version: 2

models:
  - name: dim_categories
    columns:
      - name: category_sk
        description: The primary key of the customer
        tests:
          - unique
          - not_null